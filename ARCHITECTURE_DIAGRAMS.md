# Project Structure Visualization

## 🎯 Application Workflow

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER INTERACTION                          │
│  1. Opens http://localhost:3000 in browser                      │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                   REACT FRONTEND (localhost:3000)               │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Navbar Component                                         │   │
│  │ ================                                         │   │
│  │ CV-JD Matcher | AI-Powered Job Application Analyzer    │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              ↓                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ MatcherForm Component                                   │   │
│  │ =====================                                   │   │
│  │ [Textarea 1: CV Text input area]                       │   │
│  │ [Textarea 2: JD Text input area]                       │   │
│  │ [Analyze Match Button] [View History Button]           │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              ↓                                   │
│                   HTTP POST Request                             │
│              POST /api/matches/predict/                         │
│              { cv_text: "...", jd_text: "..." }                │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                  DJANGO REST API (localhost:8000)               │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ api/views.py - MatchResultViewSet                       │   │
│  │ ========================================                 │   │
│  │ @action(detail=False, methods=['post'])                │   │
│  │ def predict(request):                                  │   │
│  │   - Validate input (PredictRequestSerializer)          │   │
│  │   - Send to ML model                                   │   │
│  │   - Save results to database                           │   │
│  │   - Return results to frontend                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              ↓                                   │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                   ML PREDICTION ENGINE                           │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ matcher/ml/predictor.py                                │   │
│  │ ================================                         │   │
│  │                                                         │   │
│  │ Step 1: Clean Text (lowercase, remove special chars)  │   │
│  │         Input: "Senior Python Engineer"                │   │
│  │         Output: "senior python engineer"              │   │
│  │                                                         │   │
│  │ Step 2: Extract Skills (matching against skill list)  │   │
│  │         Input: cleaned text                           │   │
│  │         Output: ["python", "engineer"]               │   │
│  │                                                         │   │
│  │ Step 3: Extract Experience (regex for years)          │   │
│  │         Input: "5 years of experience"               │   │
│  │         Output: 5                                     │   │
│  │                                                         │   │
│  │ Step 4: Extract Education (degree level)              │   │
│  │         Input: "Bachelor's degree in CS"             │   │
│  │         Output: "Bachelor"                           │   │
│  │                                                         │   │
│  │ Step 5: Calculate Skill Match %                       │   │
│  │         CV skills ∩ JD skills / JD skills * 100       │   │
│  │         Result: 75%                                   │   │
│  │                                                         │   │
│  │ Step 6: Calculate Experience Match %                  │   │
│  │         min(CV years / JD years, 1.0) * 100           │   │
│  │         Result: 90%                                   │   │
│  │                                                         │   │
│  │ Step 7: Calculate Education Match %                   │   │
│  │         Based on degree ranking                       │   │
│  │         Result: 100%                                  │   │
│  │                                                         │   │
│  │ Step 8: Calculate Semantic Similarity                 │   │
│  │         - Load BERT model (all-MiniLM-L6-v2)         │   │
│  │         - Encode CV text → 384-dim vector            │   │
│  │         - Encode JD text → 384-dim vector            │   │
│  │         - Cosine similarity between vectors * 100     │   │
│  │         Result: 85%                                   │   │
│  │                                                         │   │
│  │ Step 9: Predict Overall Score                         │   │
│  │         Load regression model                         │   │
│  │         X = [75%, 90%, 100%, 85%]                    │   │
│  │         model.predict(X) → 88%                       │   │
│  │         Result: 88%                                   │   │
│  │                                                         │   │
│  │ Return: {                                             │   │
│  │   "skill_match": 75.0,                               │   │
│  │   "experience_match": 90.0,                          │   │
│  │   "education_match": 100.0,                          │   │
│  │   "semantic_similarity": 85.0,                       │   │
│  │   "overall_match": 88.0                              │   │
│  │ }                                                     │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    DATABASE (SQLite)                             │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ db.sqlite3                                              │   │
│  │ ===========                                              │   │
│  │                                                         │   │
│  │ MatchResult Table                                      │   │
│  │ ────────────────                                       │   │
│  │ id: 1                                                  │   │
│  │ cv_text: "Senior Python Engineer..."                 │   │
│  │ jd_text: "Senior Engineer ML team..."                │   │
│  │ skill_match: 75.0                                     │   │
│  │ experience_match: 90.0                                │   │
│  │ education_match: 100.0                                │   │
│  │ semantic_similarity: 85.0                             │   │
│  │ overall_match: 88.0                                   │   │
│  │ created_at: 2024-01-15 10:30:00                       │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              API RESPONSE (JSON) → Django → React               │
│                                                                   │
│  HTTP 201 Created + {matching scores}                           │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                   REACT FRONTEND (Display Results)              │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ ResultsDisplay Component                               │   │
│  │ ============================                            │   │
│  │                                                         │   │
│  │  Overall Match: 88%                                   │   │
│  │  ┌──────────────────────────────────────────────┐    │   │
│  │  │ ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐ │   │   │
│  │  │ │ Skills │  │ Exp.   │  │ Edu.   │  │Semantic│ │   │   │
│  │  │ │ 75%    │  │ 90%    │  │ 100%   │  │ 85%    │ │   │   │
│  │  │ │ 🛠️     │  │ 💼     │  │ 🎓     │  │ 🧠     │ │   │   │
│  │  │ └────────┘  └────────┘  └────────┘  └────────┘ │   │   │
│  │  └──────────────────────────────────────────────┘    │   │
│  │                                                         │   │
│  │  Score Breakdown:                                     │   │
│  │  Skills:      75% [████████░░░░░░░░░]                │   │
│  │  Experience:  90% [██████████████░░░]                │   │
│  │  Education:  100% [██████████████████]               │   │
│  │  Semantic:    85% [███████████████░░░]               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ History Component (on click "View History")            │   │
│  │ ================================================         │   │
│  │ Shows previous analyses in a list                      │   │
│  │ Each item shows score badge and text previews         │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📁 File Organization

```
Project Structure
│
├── Configuration & Setup
│   ├── requirements.txt          ← Python packages
│   ├── package.json              ← Node packages
│   ├── .env.example              ← Environment template
│   └── .gitignore                ← Git ignore rules
│
├── Documentation
│   ├── README.md                 ← Full guide
│   ├── QUICKSTART.md             ← Quick setup
│   ├── START_HERE.md             ← First steps
│   ├── WELCOME.md                ← Overview
│   ├── PROJECT_SUMMARY.md        ← Architecture
│   ├── API_TESTING.md            ← API guide
│   ├── INSTALLATION_CHECKLIST.md ← Verification
│   └── FINAL_SUMMARY.md          ← Complete summary
│
├── Startup Scripts (Windows)
│   ├── start_all.bat             ← Start everything
│   ├── start_backend.bat         ← Backend only
│   └── start_frontend.bat        ← Frontend only
│
├── ML Model
│   └── overall_match_regression_model.pkl  ← Your trained model
│
├── Backend (Django)
│   └── backend/
│       ├── cvmatcher/            ← Django config
│       │   ├── settings.py       ← Configuration
│       │   ├── urls.py           ← URL routing
│       │   ├── wsgi.py           ← Server
│       │   └── asgi.py           ← Async
│       │
│       ├── api/                  ← Main app
│       │   ├── models.py         ← Database schema
│       │   ├── views.py          ← API endpoints
│       │   ├── serializers.py    ← Data validation
│       │   ├── urls.py           ← API routes
│       │   └── admin.py          ← Admin panel
│       │
│       ├── matcher/ml/           ← ML integration
│       │   └── predictor.py      ← Prediction logic
│       │
│       ├── manage.py             ← Django CLI
│       ├── requirements.txt      ← Dependencies
│       ├── verify_setup.py       ← Setup checker
│       └── db.sqlite3            ← Database (created)
│
└── Frontend (React)
    └── frontend/
        ├── public/
        │   └── index.html        ← HTML template
        │
        ├── src/
        │   ├── components/       ← React components
        │   │   ├── Navbar/       ← Navigation
        │   │   ├── MatcherForm/  ← Input form
        │   │   ├── ResultsDisplay/ ← Results view
        │   │   └── History/      ← History view
        │   │
        │   ├── App.js            ← Main component
        │   ├── index.js          ← Entry point
        │   └── CSS files         ← Styling
        │
        └── package.json          ← Dependencies
```

---

## 🔄 Data Flow Diagram

```
┌─────────┐
│ User    │
│ Browser │
└────┬────┘
     │ Open http://localhost:3000
     ↓
┌─────────────────────────┐
│ React App Loads         │
│ - Navbar Component      │
│ - MatcherForm Component │
│ - State initialized     │
└────┬────────────────────┘
     │
     │ User fills form & clicks "Analyze"
     ↓
┌──────────────────────────────────┐
│ Form Validation                  │
│ - Check CV text not empty        │
│ - Check JD text not empty        │
│ - Check minimum length           │
└────┬─────────────────────────────┘
     │ Valid data
     ↓
┌──────────────────────────────────────┐
│ Axios HTTP POST Request              │
│ POST /api/matches/predict/           │
│ Body: { cv_text, jd_text }          │
└────┬─────────────────────────────────┘
     │ 
     ↓ Network
     
┌──────────────────────────────────────┐
│ Django REST API Endpoint             │
│ api/views.py::predict()              │
│ - Deserialize request                │
│ - Validate data                      │
└────┬─────────────────────────────────┘
     │ Send to ML
     ↓
┌──────────────────────────────────────┐
│ ML Prediction                        │
│ matcher/ml/predictor.py              │
│ - Clean text                         │
│ - Extract skills                     │
│ - Extract experience                 │
│ - Extract education                  │
│ - Calculate similarities             │
│ - Call regression model              │
│ - Return scores                      │
└────┬─────────────────────────────────┘
     │ Scores calculated
     ↓
┌──────────────────────────────────────┐
│ Save to Database                     │
│ - Create MatchResult object          │
│ - Save all scores                    │
│ - Record timestamp                   │
└────┬─────────────────────────────────┘
     │ Serialize response
     ↓
┌──────────────────────────────────────┐
│ JSON Response (HTTP 201 Created)     │
│ {                                    │
│   "id": 1,                          │
│   "skill_match": 75.0,              │
│   "experience_match": 90.0,         │
│   "education_match": 100.0,         │
│   "semantic_similarity": 85.0,      │
│   "overall_match": 88.0,            │
│   "created_at": "2024-01-15..."    │
│ }                                    │
└────┬─────────────────────────────────┘
     │ 
     ↓ Network
     
┌──────────────────────────────────────┐
│ React Receives Response              │
│ - Update component state             │
│ - Clear input fields                 │
└────┬─────────────────────────────────┘
     │ Render new component
     ↓
┌──────────────────────────────────────┐
│ ResultsDisplay Component             │
│ - Show overall percentage            │
│ - Display 4 metric cards             │
│ - Show score breakdown               │
│ - Smooth animations                  │
└────┬─────────────────────────────────┘
     │ User sees results!
     ↓
┌─────────────┐
│ User Happy! │
└─────────────┘
```

---

## 🔌 Component Hierarchy

```
<App>
├── <Navbar />
│   └── Shows: CV-JD Matcher title + subtitle
│
├── {showHistory ? (
│   └── <History />
│       ├── Fetches from /api/matches/history/
│       ├── Maps results to list items
│       └── Shows: Previous matches with previews
│   ) : (
│   ├── <MatcherForm />
│   │   ├── Input: CV text
│   │   ├── Input: JD text
│   │   ├── Button: "Analyze Match"
│   │   ├── Button: "View History"
│   │   ├── Event: onSubmit → POST /api/matches/predict/
│   │   └── Shows: Error messages if validation fails
│   │
│   └── {matchResult && (
│       └── <ResultsDisplay />
│           ├── Overall Score Card
│           │   └── Shows: Overall match % with color
│           ├── Score Cards Grid
│           │   ├── Skill Match Card (🛠️)
│           │   ├── Experience Match Card (💼)
│           │   ├── Education Match Card (🎓)
│           │   └── Semantic Similarity Card (🧠)
│           └── Score Breakdown Section
│               ├── Skills breakdown bar
│               ├── Experience breakdown bar
│               ├── Education breakdown bar
│               └── Semantic breakdown bar
│       )}
│   )}
```

---

## 📊 State Management Flow

```
App State
│
├── matchResult
│   ├── Initial: null
│   ├── On Analyze: Updated with prediction response
│   └── On Clear: Set back to null
│
├── showHistory
│   ├── Initial: false
│   ├── On "View History": true
│   └── On "Back": false
│
├── loading
│   ├── Initial: false
│   ├── On Submit: true
│   ├── On Response: false
│   └── On Error: false
│
└── error (in MatcherForm)
    ├── Initial: ""
    ├── On Validation Error: error message
    ├── On API Error: error details
    └── On Submit: cleared
```

---

## 🔑 Key Integration Points

```
Frontend → Backend Communication
│
├── POST /api/matches/predict/
│   ├── Triggered: Form submit
│   ├── Data: { cv_text, jd_text }
│   ├── Response: Prediction scores
│   └── Error Handling: Show error message
│
└── GET /api/matches/history/
    ├── Triggered: Click "View History"
    ├── Data: None
    ├── Response: Array of MatchResult objects
    └── Error Handling: Show error message

Database ← Backend Updates
│
├── Create MatchResult
│   ├── Triggered: After prediction
│   ├── Data: All scores + timestamps
│   └── Response: Created object with ID
│
└── Read MatchResult
    ├── Triggered: History view
    ├── Query: Last 50 results
    └── Response: Array of objects
```

All systems connected! 🔗
